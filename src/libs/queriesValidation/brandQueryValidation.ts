export const verifyNameBrand: string = "SELECT * FROM marca WHERE nombreMarca = ?"

export const verifyIdBrand: string = "SELECT * FROM marca WHERE idMarca = ? WHERE estadoMarca = 1"

export const verifyAllIdBrand: string = "SELECT * FROM marca WHERE idMarca = ?"