
export const verifyIdSale: string = "SELECT * FROM venta WHERE idVenta = ?"

export const verifyIdProduct: string = "SELECT * FROM producto WHERE idProducto = ?"

export const verifyNumSale: string = "SELECT * FROM detalle_venta WHERE numVenta = ?"