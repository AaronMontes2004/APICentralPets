export const verifyIdHistorial: string = "SELECT * FROM historial_medico WHERE idHistorial = ?"

export const verifyIdDiagnostic: string = "SELECT * FROM diagnostico WHERE idDiagnostico = ?"

export const verifyIdTreatment: string = "SELECT * FROM tratamiento WHERE idTratamiento = ?"

export const verifyIdUser: string = "SELECT * FROM usuario WHERE idUsuario = ?"

export const verifyIdPet: string = "SELECT * FROM mascota WHERE idMascota = ?"

